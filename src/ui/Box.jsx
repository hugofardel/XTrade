import styled from "styled-components";

const Box = styled.div`
	position: relative;
	background-color: var(--color-grey);
	border-radius: 12px;
	padding: 1rem 2rem;

	h2 {
		text-align: left;
		font-weight: 500;
	}
`;

export default Box;
